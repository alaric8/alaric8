# .github/workflows/trigger-workflow.yml
name: Triggered Workflow

on:
  repository_dispatch:
    types: [trigger]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Print repository name
        run: echo "Triggered by repository dispatch event"
